# Waitlist Monitor

A simple waitlist monitor with a FastAPI backend and a vanilla JS frontend.

## Setup

1. Backend:
   ```bash
   cd backend
   pip install -r requirements.txt
   uvicorn app.main:app --reload
   ```

2. Frontend:
   Open `frontend/index.html` in your browser.
